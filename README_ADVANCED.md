# 🚀 GT-Land Manager Ver 9

<div align="center">

![Version](https://img.shields.io/badge/version-Ver%209-blue.svg)
![Python](https://img.shields.io/badge/python-3.13-green.svg)
![PyQt6](https://img.shields.io/badge/PyQt6-6.10.0-orange.svg)
![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)

**سیستم پیشرفته مدیریت و Export داده‌های Google Sheets به Excel**

</div>

---

## ✨ ویژگی‌های نسخه Ver 9

### 🎯 قابلیت‌های جدید

#### 1. **Advanced Template System با Drag & Drop**
- 📊 **Column Mapping** حرفه‌ای با ویجت Drag & Drop
- 🔗 ربط دادن بصری ستون‌های Google Sheets به Excel
- 📄 پشتیبانی از چند Google Sheet به صورت همزمان
- ⚡ **Formula Builder** برای تبدیل و پردازش داده‌ها

#### 2. **Formula Builder پیشرفته**
- 🔗 **Merge** - ادغام چند ستون
- ✂️ **Trim** - حذف فاصله‌های اضافی (Trim, RTrim, LTrim)
- 🔤 **Case Transform** - تبدیل حروف (Upper/Lower/Title)
- 🔄 **Replace** - جایگزینی متن
- ➕ **Math Operations** - عملیات ریاضی (جمع، تفریق، ضرب، تقسیم، درصد، توان)
- 📅 **Date Format** - قالب‌بندی تاریخ (میلادی/شمسی)
- ❓ **IF Condition** - شرط‌گذاری
- 📏 **Substring** - برش متن
- 🔢 **Number Format** - قالب‌بندی اعداد (جدا کننده هزار، اعشار، پول)

#### 3. **Wizard Interface**
- 🧙 ویزارد 5 مرحله‌ای برای ساخت Template:
  1. انتخاب فایل Excel نمونه
  2. تحلیل فایل و نمایش ستون‌ها
  3. انتخاب Google Sheets مبدا
  4. Mapping ستون‌ها با Drag & Drop
  5. تنظیمات نهایی و ذخیره

---

## 📦 نصب و راه‌اندازی

### پیش‌نیازها
```bash
Python 3.13+
pip
virtualenv (اختیاری)
```

### مراحل نصب

1. **Clone کردن پروژه:**
```bash
git clone https://github.com/YOUR_USERNAME/GT-Land.git
cd GT-Land
```

2. **ساخت Virtual Environment:**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

3. **نصب کتابخانه‌ها:**
```bash
pip install -r requirements.txt
```

4. **راه‌اندازی دیتابیس:**
```bash
python setup_database.py
```

5. **اجرای برنامه:**
```bash
python app/main.py
```

یا استفاده از فایل Batch:
```bash
GT-Land.bat
```

---

## 🎨 نحوه استفاده از Template System

### مرحله 1: ساخت Template جدید

1. از منوی اصلی **"مدیریت Template ها"** را انتخاب کنید
2. روی دکمه **"➕ ساخت Template جدید"** کلیک کنید
3. ویزارد باز می‌شود:

#### **صفحه 1: انتخاب فایل Excel**
- فایل Excel نمونه (از نرم‌افزار مالیاتی) را انتخاب کنید
- فایل باید فرمت `.xlsx` یا `.xls` باشد
- ردیف اول باید عناوین ستون‌ها باشد

#### **صفحه 2: تحلیل فایل**
- برنامه فایل را تحلیل می‌کند
- تعداد Sheet ها و ستون‌ها نمایش داده می‌شود
- عناوین ستون‌ها لیست می‌شوند

#### **صفحه 3: انتخاب Google Sheets**
- Google Sheet هایی که داده از آن‌ها استخراج می‌شود را انتخاب کنید
- می‌توانید چند Sheet انتخاب کنید
- اطلاعات هر Sheet (نام و تعداد ستون‌ها) نمایش داده می‌شود

#### **صفحه 4: Mapping با Drag & Drop**
- **سمت راست**: ستون‌های Google Sheet (بکشید)
- **سمت چپ**: ستون‌های Excel (رها کنید)

**نحوه استفاده:**
1. ستون مورد نظر از Google Sheet را بگیرید
2. روی ستون مقصد در Excel رها کنید
3. خط اتصال نمایش داده می‌شود ✅
4. برای افزودن Formula:
   - ستون Excel را انتخاب کنید
   - روی **"⚡ افزودن Formula"** کلیک کنید
   - نوع Formula را انتخاب کنید
   - تنظیمات را وارد کنید
   - ذخیره کنید

**مثال Mapping:**
```
Excel Column A  ←  Google Sheet "فروش" → "کد کالا"
Excel Column B  ←  Google Sheet "فروش" → "نام کالا" [+Formula: UPPER]
Excel Column C  ←  Google Sheet "انبار" → "موجودی" [+Formula: NUMBER_FORMAT]
```

#### **صفحه 5: تنظیمات نهایی**
- نام Template را وارد کنید (مثلاً: "سامانه مؤدیان 1402")
- نوع Template را انتخاب کنید (type1, type2, type3, custom)
- توضیحات اختیاری بنویسید
- خلاصه Template نمایش داده می‌شود
- روی **"Finish"** کلیک کنید

### مرحله 2: استفاده از Template

1. از منوی اصلی **"Export به Excel"** را انتخاب کنید
2. Template ساخته شده را از لیست انتخاب کنید
3. مکان ذخیره فایل را مشخص کنید
4. روی **"Export"** کلیک کنید

برنامه به صورت خودکار:
- داده‌ها را از Google Sheets مشخص شده استخراج می‌کند
- Mapping ها را اعمال می‌کند
- Formula ها را روی داده‌ها اجرا می‌کند
- نتیجه را در فایل Excel ذخیره می‌کند

---

## 📊 Formula Examples

### 1. Merge چند ستون
```
ستون "کد کالا" + "-" + ستون "نام کالا"
→ "1234 - لپ تاپ"
```

### 2. Trim + Upper
```
"  علی رضایی  " → "علی رضایی" → "علی رضایی"
```

### 3. Math Operations
```
قیمت × 1.09  (افزودن 9% مالیات)
```

### 4. Date Format
```
"2025/11/08" → "1404/08/17" (تاریخ شمسی)
```

### 5. IF Condition
```
IF موجودی > 0 THEN "موجود" ELSE "ناموجود"
```

### 6. Substring
```
"1234567890" → از کاراکتر 0, طول 4 → "1234"
```

### 7. Number Format
```
123456789 → "123,456,789" (جدا کننده هزار)
```

---

## 🏗️ ساختار پروژه

```
GT-Land/
├── app/
│   ├── core/              # هسته برنامه
│   │   ├── database.py    # مدیریت دیتابیس + Template management
│   │   ├── google_sheets.py
│   │   └── excel_exporter.py
│   ├── gui/               # رابط کاربری
│   │   ├── dialogs/
│   │   │   ├── template_manager_dialog_advanced.py  # مدیریت Template (جدید)
│   │   │   └── formula_builder_dialog.py            # ساخت Formula (جدید)
│   │   ├── widgets/
│   │   │   └── column_mapping_widget.py             # Drag & Drop Widget (جدید)
│   │   └── main_window.py
│   ├── models/            # مدل‌های دیتابیس
│   └── utils/             # توابع کمکی
├── data/                  # داده‌ها
│   ├── exports/           # فایل‌های خروجی
│   └── backups/           # Backup دیتابیس
├── logs/                  # Log فایل‌ها
├── templates/             # Template های Excel
├── config/                # تنظیمات
│   └── credentials.json   # Google API Credentials
├── requirements.txt       # کتابخانه‌های مورد نیاز
├── setup_database.py      # نصب دیتابیس
└── GT-Land.bat            # راه‌انداز ساده
```

---

## 🔧 تنظیمات پیشرفته

### Google Sheets API
1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. یک پروژه جدید بسازید
3. Google Sheets API را فعال کنید
4. Credentials ایجاد کنید (Service Account)
5. فایل JSON را دانلود و در `config/credentials.json` قرار دهید

### Database
- دیتابیس SQLite در `data/gt_land.db`
- Backup خودکار در `data/backups/`
- برای reset: `python reset_database.py`

---

## 📝 Changelog

### Ver 9 (2025-11-08)
- ✨ **NEW**: Advanced Template System با Wizard Interface
- ✨ **NEW**: Drag & Drop Column Mapping Widget
- ✨ **NEW**: Formula Builder با 9 نوع Formula
- ✨ **NEW**: پشتیبانی از چند Google Sheet به صورت همزمان
- 🔧 **IMPROVED**: UI/UX بهبود یافته
- 🔧 **IMPROVED**: مدیریت Template ها در دیتابیس
- 📚 **DOCS**: مستندات کامل به‌روزرسانی شد

### Ver 1.0.0 (قبلی)
- پایه برنامه
- استخراج از Google Sheets
- Export به Excel ساده
- مدیریت داده‌ها

---

## 🤝 مشارکت

برای مشارکت در پروژه:
1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را Commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request بزنید

---

## 📄 License

این پروژه تحت لایسنس MIT منتشر شده است.

---

## 👨‍💻 توسعه دهنده

**علیرضا حامد**
- 📧 Email: your-email@example.com
- 💼 GitHub: [@your-username](https://github.com/your-username)

---

## 🙏 تشکر

از استفاده شما از GT-Land Manager متشکریم! 

برای گزارش باگ یا پیشنهاد ویژگی جدید، لطفاً Issue ایجاد کنید.

---

<div align="center">

**ساخته شده با ❤️ توسط علیرضا حامد**

[⬆ برگشت به بالا](#-gt-land-manager-ver-9)

</div>
