# 🔧 راهنمای اصلاحات انجام شده

تاریخ: 2025/11/12  
**آخرین بروزرسانی: 2025/11/12 - 15:30**

## ✅ مشکلات حل شده

### 1️⃣ خطای VS Code Out of Memory (OOM)

**مشکل:** VS Code به خاطر تحلیل فایل‌های زیاد (پایتون، دیتابیس، لاگ‌ها) از حافظه خارج می‌شد

**راه حل:**
- فایل `.vscode/settings.json` ایجاد/بروزرسانی شد
- تنظیمات زیر اضافه شدند:
  ```json
  "python.analysis.memory.keepLibraryAst": false
  "python.analysis.diagnosticMode": "openFilesOnly"
  "python.analysis.indexing": false
  ```
- فولدرهای زیر از نظارت VS Code خارج شدند:
  - `__pycache__`
  - `venv`
  - `logs`
  - `data/exports`
  - `*.db`

**توصیه:** اگر دوباره خطا داد:
1. VS Code را کاملاً ببندید
2. فایل `.vscode/settings.json` را حفظ کنید
3. VS Code را دوباره باز کنید

---

### 2️⃣ مشکل خواندن هدرهای Ready و Extracted

**مشکل:** وقتی نام ستون‌ها در گوگل شیت را تغییر می‌دادید به `Ready` و `Extracted`، برنامه آنها را نمی‌خواند

**علت:** کد قبلی فقط نام دقیق با حروف بزرگ/کوچک یکسان را جستجو می‌کرد

**راه حل:**
- در فایل `app/core/google_sheets.py` جستجوی هوشمند اضافه شد:
  - Case-insensitive (حروف بزرگ/کوچک مهم نیست)
  - Trim شده (فضای خالی اول و آخر نادیده گرفته می‌شود)
  - لاگ‌های بهتر برای debug

**حالا این نام‌ها کار می‌کنند:**
- ✅ `Ready` یا `ready` یا `READY`
- ✅ `Extracted` یا `extracted` یا `EXTRACTED`
- ✅ `  Ready  ` (با فضای خالی)

**توصیه استاندارد:**
- نام ستون آماده: `Ready` (بدون علامت، بدون فضای خالی)
- نام ستون استخراج: `Extracted` (بدون علامت، بدون فضای خالی)

---

### 3️⃣ مشکل حذف تنظیمات شیت به جای داده‌ها

**مشکل:** وقتی از تب "داده‌ها" شیت‌ها را حذف می‌کردید، تنظیمات شیت هم پاک می‌شد

**علت:** متد `delete_sheet_data` در دیتابیس وجود نداشت

**راه حل:**
1. **متد جدید در دیتابیس** (`app/core/database.py`):
   - `delete_sheet_data(sheet_config_id)` - فقط داده‌ها را حذف می‌کند
   - `delete_sheet_config(sheet_config_id)` - داده‌ها + تنظیمات را حذف می‌کند

2. **دو دکمه جدا در UI** (`app/gui/widgets/data_viewer_widget.py`):
   - 🗑️ **حذف فقط داده‌ها** (نارنجی) - داده‌ها حذف، تنظیمات باقی
   - 💣 **حذف کامل** (قرمز) - هر دو حذف می‌شوند

3. **پیام‌های واضح:**
   - حذف داده‌ها: "تنظیمات شیت حفظ می‌شود"
   - حذف کامل: "باید دوباره شیت‌ها را تعریف کنید"

---

## 📋 دکمه‌های صفحه داده‌ها

| دکمه | رنگ | عملکرد | تأثیر بر تنظیمات |
|------|-----|--------|------------------|
| ☑️ انتخاب همه | آبی | انتخاب تمام کارت‌ها | - |
| ⬜ لغو انتخاب | خاکستری | لغو انتخاب | - |
| 🗑️ حذف فقط داده‌ها | نارنجی | حذف رکوردهای استخراج شده | ✅ حفظ می‌شود |
| 💣 حذف کامل | قرمز | حذف داده + تنظیمات | ❌ حذف می‌شود |

---

## 🧪 تست عملکرد

### تست 1: خواندن هدرهای جدید
1. در گوگل شیت خود، ردیف اول را به `Ready` و `Extracted` تغییر دهید
2. در برنامه، به تب "استخراج" بروید
3. شیت را انتخاب کنید و "شروع استخراج" بزنید
4. باید رکوردها را بخواند و لاگ نشان دهد: `✅ ستون آماده پیدا شد: 'Ready'`

### تست 2: حذف فقط داده‌ها
1. به تب "داده‌ها و Export" بروید
2. یک شیت را انتخاب کنید
3. دکمه "🗑️ حذف فقط داده‌ها" را بزنید
4. تأیید کنید
5. به تب "تنظیمات" بروید - شیت باید هنوز آنجا باشد ✅

### تست 3: حذف کامل
1. به تب "داده‌ها و Export" بروید
2. یک شیت را انتخاب کنید
3. دکمه "💣 حذف کامل" را بزنید
4. پیام هشدار را بخوانید (قرمز و خطرناک)
5. تأیید کنید
6. به تب "تنظیمات" بروید - شیت نباید وجود داشته باشد ❌

---

## 🐛 اگر مشکلی پیش آمد

### خطای "ستون یافت نشد"
1. به فایل `logs/app.log` نگاه کنید
2. خط شروع با `📊 هدرهای یافت شده:` را پیدا کنید
3. بررسی کنید نام ستون‌های شما دقیقاً چیست
4. در تنظیمات شیت، همان نام را وارد کنید (case-insensitive)

### VS Code همچنان خطای OOM می‌دهد
```powershell
# تمیز کردن کامل کش
Remove-Item -Recurse -Force .vscode-server 2>$null
Remove-Item -Recurse -Force **/__pycache__ -ErrorAction SilentlyContinue
Remove-Item *.db-journal -ErrorAction SilentlyContinue

# راه‌اندازی مجدد
code .
```

### برنامه شیت‌ها را نمی‌خواند
1. فایل `logs/app.log` را باز کنید
2. دنبال خط‌های `ERROR` بگردید
3. اگر `PERMISSION_DENIED` بود:
   - فایل `credentials.json` را بررسی کنید
   - دسترسی Service Account را در گوگل شیت چک کنید

---

## 📝 تغییرات فایل‌ها

### فایل‌های تغییر یافته:
1. ✅ `.vscode/settings.json` - تنظیمات بهینه‌سازی حافظه
2. ✅ `app/core/google_sheets.py` - جستجوی هوشمند هدرها
3. ✅ `app/core/database.py` - اضافه شدن `delete_sheet_data()`
4. ✅ `app/gui/widgets/data_viewer_widget.py` - جداسازی دکمه‌های حذف

### فایل‌های بدون تغییر:
- تمام مدل‌ها (models)
- تمام دیالوگ‌ها (dialogs)
- سایر ویجت‌ها

---

## 💡 نکات مهم

1. **همیشه از دکمه "حذف فقط داده‌ها" استفاده کنید** مگر اینکه واقعاً می‌خواهید شیت را کاملاً حذف کنید

2. **نام ستون‌های استاندارد:**
   - آماده: `Ready`
   - استخراج شده: `Extracted`
   - بدون علامت، بدون فاصله اضافی

3. **قبل از استخراج:**
   - مطمئن شوید تنظیمات شیت صحیح است
   - یک بار "تست اتصال" بزنید
   - لاگ‌ها را بررسی کنید

4. **برای کاهش مصرف حافظه VS Code:**
   - فایل‌های بزرگ `.db` را باز نکنید
   - فولدر `logs` را در VS Code باز نکنید
   - اگر نیاز نیست، پلاگین‌های اضافی را غیرفعال کنید

---

## 🎯 خلاصه

| مشکل | وضعیت | راه حل |
|------|-------|--------|
| VS Code OOM | ✅ حل شد | `.vscode/settings.json` |
| هدرها خوانده نمی‌شوند | ✅ حل شد | جستجوی case-insensitive + اولویت نام |
| تنظیمات پاک می‌شوند | ✅ حل شد | دو دکمه جدا |
| Batch Update Timeout | ✅ حل شد | تقسیم به 500 رکورد + retry |
| عدم گزارش پیشرفت | ✅ حل شد | لاگ فارسی رنگی زنده |

---

## 🚀 قابلیت‌های جدید (2025/11/12 - 15:30)

### **4️⃣ سیستم Batch Update هوشمند**

**مشکل:** با فایل‌های بزرگ (40,000+ ردیف) خطای Connection Timeout رخ می‌داد

**راه حل:**

1. **تقسیم‌بندی هوشمند:**
   ```python
   BATCH_SIZE = 500  # هر بچ 500 رکورد
   total_batches = (total_rows + 499) // 500
   ```

2. **Retry با Exponential Backoff:**
   - تلاش 1: صبر 2 ثانیه
   - تلاش 2: صبر 4 ثانیه
   - تلاش 3: صبر 8 ثانیه

3. **تقسیم خودکار در صورت خطا:**
   ```
   اگر بچ 500 تایی خطا داد:
   → تقسیم به 2 بچ 250 تایی
   → اگر باز خطا داد → تقسیم به 4 بچ 125 تایی
   → تا رسیدن به حداقل 10 رکورد
   ```

4. **لاگ دقیق:**
   ```
   📦 بچ 1/85: 500 ردیف (از 1 تا 500)
   ✅ بچ 1 موفق: 500 ردیف
   📦 بچ 2/85: 500 ردیف (از 501 تا 1000)
   ...
   ```

**نتیجه:**
- ✅ عدم قطع شدن در فایل‌های بزرگ
- ✅ ادامه کار حتی با خطاهای موقت
- ✅ گزارش دقیق بچ‌های ناموفق

---

### **5️⃣ لاگ فارسی رنگی زنده**

**مشکل:** کاربر نمی‌دانست سیستم در حال کار است یا هنگ کرده

**راه حل - صفحه لاگ پیشرفته:**

#### **رنگ‌بندی:**
- 🔵 **آبی روشن** (`#00E5FF`): اطلاعات عمومی
- 🟢 **سبز روشن** (`#00FF41`): عملیات موفق
- 🟡 **زرد/نارنجی** (`#FFC107`): هشدارها
- 🔴 **قرمز** (`#FF1744`): خطاها

#### **نوار پیشرفت دو سطحی:**
1. **پیشرفت کلی:** چند درصد از کل شیت‌ها
2. **پیشرفت جزئی:** چند رکورد از شیت فعلی

#### **لاگ لحظه‌ای:**
```
================================================================
🕐 زمان شروع: 2025/11/12 - 15:14:45
================================================================

🔍 دریافت لیست شیت‌های فعال از دیتابیس...
✅ 4 شیت فعال یافت شد
📌 استخراج از تمام 4 شیت فعال

────────────────────────────────────────────────────────────
📊 شیت 1/4: Gift Bank(PUBG🔫)
🔗 URL: https://docs.google.com/spreadsheets/d/...
📄 Worksheet: Archived in 19-10
────────────────────────────────────────────────────────────
  📥 استخراج: 100/43,771 - بارگذاری داده‌ها
  📥 استخراج: 500/43,771 - پردازش رکوردها
  📥 استخراج: 1,000/43,771 - ذخیره در دیتابیس
  ...
  📦 بچ 1/85: 500 ردیف (از 1 تا 500)
  ✅ بچ 1 موفق: 500 ردیف
  📦 بچ 2/85: 500 ردیف (از 501 تا 1000)
  ✅ بچ 2 موفق: 500 ردیف
  ...
  ✅ نتیجه: 42,854 رکورد جدید، 0 بروزرسانی، 42,854 ردیف استخراج شد
  ✅ علامت‌گذاری: 42,854 ردیف

================================================================
🎯 خلاصه نتایج نهایی
================================================================
⏱️  مدت زمان: 245.3 ثانیه (4.1 دقیقه)
📁 شیت‌های پردازش شده: 4
📥 ردیف‌های استخراج شده: 43,771
➕ رکوردهای جدید: 42,854
🔄 رکوردهای بروز شده: 0
⚠️  تکراری‌ها (نیاز به بررسی): 917
❌ خطاها: 0
⚡ سرعت استخراج: 178.4 ردیف/ثانیه
================================================================
🕐 زمان پایان: 2025/11/12 - 15:18:50
================================================================
✅ لاگ عملیات در دیتابیس ثبت شد (ID: 42)
```

#### **ویژگی‌های لاگ:**
- ✅ Auto-scroll به انتها
- ✅ پشتیبانی RTL
- ✅ فونت Tahoma/Consolas
- ✅ کپی/پیست متن
- ✅ ذخیره خودکار در دیتابیس

---

### **6️⃣ مدیریت پیشرفته خطا**

#### **انواع خطا و راه حل:**

| نوع خطا | تشخیص | راه حل خودکار |
|---------|-------|---------------|
| **Connection Timeout** | `timeout` در پیام | تقسیم بچ به دو نیمه |
| **Connection Aborted** | `aborted` در پیام | Retry با تاخیر 2-8 ثانیه |
| **Rate Limit** | کد 429 | صبر 60 ثانیه و retry |
| **Sheet Not Found** | `SpreadsheetNotFound` | گزارش خطا و continue |
| **Permission Denied** | `PERMISSION_DENIED` | گزارش خطا و skip |

#### **سطوح Fallback:**
```
تلاش 1: بچ 500 رکورد → خطا
  ↓
تلاش 2: تقسیم به 2×250 → خطا در یکی
  ↓
تلاش 3: تقسیم به 4×125 → همه موفق ✅
```

---

## 📊 آمار عملکرد

### **قبل از بهینه‌سازی:**
- ❌ فایل 40,000 ردیف: Connection Timeout بعد از 15,000
- ❌ هیچ گزارشی از پیشرفت
- ❌ باید دستی retry کنید

### **بعد از بهینه‌سازی:**
- ✅ فایل 43,771 ردیف: تمام شد (4.1 دقیقه)
- ✅ گزارش لحظه‌ای هر 100 رکورد
- ✅ Retry خودکار با موفقیت 100%
- ✅ سرعت: 178 ردیف/ثانیه

---

## 🔧 تنظیمات پیشنهادی

### **برای فایل‌های خیلی بزرگ (100,000+ ردیف):**

در `app/core/google_sheets.py` خط 276:
```python
BATCH_SIZE = 300  # کاهش به 300 (پیش‌فرض 500)
```

### **برای شبکه کند:**

خط 333:
```python
wait_time = 3 ** attempt  # 3, 9, 27 ثانیه (پیش‌فرض 2, 4, 8)
```

---

## 🧪 تست کردن

### **تست 1: فایل بزرگ (40,000+ ردیف)**
```
✅ باید بدون خطا تمام شود
✅ لاگ باید هر 500 رکورد بروز شود
✅ زمان تقریبی: 3-5 دقیقه
```

### **تست 2: قطع شبکه موقت**
```
✅ باید خودکار retry کند
✅ لاگ نشان دهد: "⚠️ تلاش 1 ناموفق، صبر 2s..."
✅ بعد از وصل شدن، ادامه دهد
```

### **تست 3: لغو عملیات**
```
⚠️ این قابلیت هنوز پیاده‌سازی نشده
📝 TODO: اضافه کردن دکمه "لغو عملیات"
```

---

## 📝 فایل‌های تغییر یافته

### **نسخه 2025/11/12 - 15:30:**
```
✅ app/core/google_sheets.py
   - اصلاح منطق پیدا کردن ستون‌ها (خط 114-175)
   - Batch update هوشمند (خط 220-340)
   - Retry با تقسیم‌بندی (خط 342-407)
   - اضافه کردن progress_callback (خط 409)

✅ app/gui/widgets/extraction_widget.py
   - Thread با لاگ کامل (خط 20-250)
   - UI لاگ رنگی (خط 800-830)
   - نوار پیشرفت دو سطحی (خط 810-820)

✅ FIX_GUIDE.md - این فایل
✅ EXTRACTION_LOGIC.md - مستندات کامل
```

---

## 🎯 خلاصه

| مشکل | وضعیت | راه حل |
|------|-------|--------|
| VS Code OOM | ✅ حل شد | `.vscode/settings.json` |
| هدرها خوانده نمی‌شوند | ✅ حل شد | جستجوی case-insensitive |
| تنظیمات پاک می‌شوند | ✅ حل شد | دو دکمه جدا |

---

**توسعه دهنده:** علیرضا حامد  
**تاریخ:** 2025/11/12  
**نسخه:** 9.0.1
