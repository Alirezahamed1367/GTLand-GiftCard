# 🚀 GT-Land Manager# 🚀 GT-Land Manager



> سیستم جامع مدیریت و پردازش داده‌های فروش Google Sheets**نسخه:** 2.2.0  

**تاریخ:** 2025-11-08

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://python.org)

[![PyQt6](https://img.shields.io/badge/PyQt6-6.10+-green.svg)](https://pypi.org/project/PyQt6/)---

[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

## 📋 درباره

---

سیستم جامع مدیریت داده از Google Sheets تا Excel:

## ✨ ویژگی‌ها

- ✅ استخراج خودکار از Google Sheets

- 📥 **استخراج هوشمند** از Google Sheets- ✅ ذخیره در دیتابیس SQLite

- 💾 **ذخیره‌سازی مرکزی** در SQLite- ✅ Export به Excel با Template

- 🔄 **مدیریت Duplicate** خودکار- ✅ مدیریت و گزارش‌گیری کامل

- 📤 **Export به Excel** با Template- ✅ **مدیریت پیشرفته دیتابیس** (جدید!)

- 📊 **گزارش‌گیری** پیشرفته- ✅ **آرشیو هوشمند** (جدید!)

- 🎨 **UI مدرن** با PyQt6- ✅ **بازیابی و بکاپ** (جدید!)



------



## 🚀 نصب سریع## 🛠️ نصب سریع



```bash```powershell

# 1. کلون پروژه# نصب کتابخانه‌ها

git clone https://github.com/your-org/GT-Land.gitpip install -r requirements.txt

cd GT-Land

# راه‌اندازی دیتابیس

# 2. محیط مجازیpython setup_database.py

python -m venv venv

venv\Scripts\activate  # Windows6. **⚙️ تنظیمات** - تنظیمات برنامه

# source venv/bin/activate  # Linux/Mac

# تنظیم Google API

# 3. نصب پکیج‌ها

pip install -r requirements.txt# فایل credentials.json را در config/ قرار دهید## 🚀 راه‌اندازی سریع



# 4. راه‌اندازی دیتابیس

python setup_database.py

# اجرا### نصب و اجرا (3 مرحله ساده)

# 5. اجرا

GT-Land.bat  # Windows.\GT-Land.bat

# python app/main.py  # Linux/Mac

``````1. **نصب محیط مجازی:**



---   - دابل کلیک روی `run.bat`



## 📚 مستندات📖 **مستندات کامل:** `USER_GUIDE.md`     - گزینه `1` را انتخاب کنید



- **[COMPLETE_GUIDE.md](COMPLETE_GUIDE.md)** - راهنمای کامل (کاربر + توسعه‌دهنده)🔧 **راهنمای Google API:** `GOOGLE_SETUP.md`

- **[USER_GUIDE.md](USER_GUIDE.md)** - راهنمای کاربری ساده

2. **راه‌اندازی دیتابیس:**

---

---   - گزینه `2` را انتخاب کنید

## 🔧 تنظیمات Google API



1. [Google Cloud Console](https://console.cloud.google.com/)

2. ایجاد پروژه → فعال‌سازی Sheets API## ✨ ویژگی‌ها3. **اجرای برنامه:**

3. ایجاد Service Account → دانلود JSON

4. کپی به `config/credentials.json`   - دابل کلیک روی `GT-Land.bat` ⭐

5. اشتراک Sheet با ایمیل Service Account

### 🔹 استخراج هوشمند

---

- استخراج خودکار با تشخیص تکرارییا:

## 📞 پشتیبانی

- پشتیبانی از چندین Sheet```powershell

- **ایمیل:** support@gtland.com

- **Telegram:** @GTLandSupport- مدیریت Conflict ها.\GT-Land.bat

- **Issues:** [GitHub](https://github.com/your-org/GT-Land/issues)

```

---

### 🔹 Export حرفه‌ای

**نسخه:** 2.2.0 | **License:** MIT | **سال:** 2025

- Template های سفارشی## 🛠️ پیش‌نیازها

- فرمت‌بندی خودکار

- Export دسته‌جمعی### نرم‌افزارها



### 🔹 مدیریت آسان1. **Python 3.9 یا بالاتر** (نصب شده در سیستم)

- فیلتر بر اساس Sheet   ```bash

- ویرایش و حذف   python --version

- جستجوی پیشرفته   ```



---2. **PostgreSQL 13 یا بالاتر**

   - دانلود از: https://www.postgresql.org/download/

## 📚 ساختار   - نصب و راه‌اندازی سرویس PostgreSQL



```3. **Git** (اختیاری)

GT-Land/

├── app/          # کد اصلی### حساب Google Cloud

├── config/       # تنظیمات

├── data/         # دیتابیس و Export ها1. ایجاد پروژه در Google Cloud Console

└── logs/         # لاگ‌ها2. فعال‌سازی Google Sheets API و Google Drive API

```3. ایجاد Service Account

4. دانلود فایل `credentials.json`

---

## 📦 نصب و راه‌اندازی

## 🆘 راهنما

### مرحله 1: آماده‌سازی محیط

- **کاربری**: `USER_GUIDE.md`

- **معماری**: `ARCHITECTURE.md````powershell

- **دستورات**: `COMMANDS.md`# رفتن به دایرکتوری پروژه

cd d:\Project\GT-Land

---

# ایجاد محیط مجازی Python

**© 2025 GT-Land Manager**python -m venv venv


# فعال‌سازی محیط مجازی
.\venv\Scripts\Activate

# ارتقا pip
python -m pip install --upgrade pip
```

### مرحله 2: نصب کتابخانه‌ها

```powershell
# نصب تمام کتابخانه‌های مورد نیاز
pip install -r requirements.txt
```

### مرحله 3: تنظیم PostgreSQL

```sql
-- اجرای دستورات زیر در PostgreSQL

-- ایجاد دیتابیس
CREATE DATABASE gt_land;

-- ایجاد کاربر (اختیاری)
CREATE USER gt_land_user WITH PASSWORD 'your_strong_password';

-- اعطای دسترسی
GRANT ALL PRIVILEGES ON DATABASE gt_land TO gt_land_user;
```

### مرحله 4: تنظیم متغیرهای محیطی

```powershell
# کپی فایل نمونه
copy .env.example .env

# ویرایش فایل .env و تنظیم مقادیر:
# - DATABASE_URL
# - DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
# - GOOGLE_CREDENTIALS_PATH
```

مثال `.env`:

```env
DATABASE_URL=postgresql://gt_land_user:your_strong_password@localhost:5432/gt_land
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gt_land
DB_USER=gt_land_user
DB_PASSWORD=your_strong_password

GOOGLE_CREDENTIALS_PATH=config/credentials.json

APP_NAME=GT-Land Manager
APP_VERSION=1.0.0
DEBUG_MODE=False
LOG_LEVEL=INFO

EXPORT_DIR=data/exports
TEMPLATE_DIR=templates
LOG_DIR=logs
```

### مرحله 5: Google Credentials

```powershell
# قرار دادن فایل credentials.json در دایرکتوری config
copy path\to\your\credentials.json config\credentials.json
```

### مرحله 6: ایجاد جداول دیتابیس

```powershell
# اجرای اسکریپت راه‌اندازی
python -c "from app.models import init_db; init_db(); print('✅ دیتابیس با موفقیت راه‌اندازی شد.')"
```

## 🚀 اجرای برنامه

```powershell
# اجرای برنامه اصلی
python app/main.py
```

## 📁 ساختار پروژه

```
GT-Land/
├── app/
│   ├── gui/                  # رابط کاربری گرافیکی
│   │   ├── main_window.py    # پنجره اصلی
│   │   ├── dashboard.py      # داشبورد
│   │   ├── sheet_manager.py  # مدیریت شیت‌ها
│   │   ├── data_extractor.py # استخراج داده
│   │   ├── export_manager.py # تولید خروجی
│   │   └── reports.py        # گزارش‌ها
│   ├── core/                 # منطق اصلی
│   │   ├── google_sheets.py  # اتصال به Google Sheets
│   │   ├── database.py       # مدیریت دیتابیس
│   │   ├── validator.py      # اعتبارسنجی
│   │   ├── excel_generator.py# تولید Excel
│   │   └── logger.py         # سیستم لاگ
│   ├── models/               # مدل‌های دیتابیس
│   │   ├── base.py
│   │   ├── sheet_config.py
│   │   ├── sales_data.py
│   │   ├── export_template.py
│   │   ├── process_log.py
│   │   └── export_log.py
│   ├── utils/                # ابزارها
│   │   ├── constants.py      # ثابت‌ها
│   │   └── helpers.py        # توابع کمکی
│   └── main.py               # نقطه شروع
├── config/                   # تنظیمات
│   └── credentials.json      # Google API
├── templates/                # تمپلیت‌های Excel
│   ├── template_type1.xlsx
│   ├── template_type2.xlsx
│   └── template_type3.xlsx
├── data/exports/             # فایل‌های خروجی
├── logs/                     # فایل‌های لاگ
├── resources/                # منابع
│   ├── icons/                # آیکون‌ها
│   └── styles/               # استایل‌ها
├── requirements.txt
├── .env
├── .gitignore
└── README.md
```

## 🎯 راهنمای استفاده

### 1. افزودن تنظیمات Google Sheet

1. از منوی سمت چپ گزینه **"مدیریت شیت‌ها"** را انتخاب کنید
2. روی دکمه **"افزودن شیت جدید"** کلیک کنید
3. اطلاعات زیر را وارد کنید:
   - نام تنظیمات
   - URL گوگل شیت
   - نام ورک‌شیت
   - نام ستون "آماده برای استخراج"
   - نام ستون "استخراج شده"
   - ستون‌های کلید یکتا
   - نقشه‌برداری ستون‌ها
4. روی **"ذخیره"** کلیک کنید

### 2. استخراج داده‌ها

1. از منوی سمت چپ گزینه **"استخراج داده"** را انتخاب کنید
2. شیت‌های مورد نظر را انتخاب کنید
3. روی **"شروع استخراج"** کلیک کنید
4. پیشرفت عملیات را مشاهده کنید
5. در صورت وجود داده تکراری، تصمیم‌گیری کنید:
   - **بروزرسانی**: داده قبلی را بروزرسانی می‌کند
   - **رد کردن**: داده جدید را نادیده می‌گیرد

### 3. تولید فایل Excel

1. از منوی سمت چپ گزینه **"تولید خروجی"** را انتخاب کنید
2. نوع تمپلیت را انتخاب کنید (Type 1, Type 2, Type 3)
3. فیلترهای مورد نظر را اعمال کنید
4. روی **"تولید فایل"** کلیک کنید
5. فایل در پوشه `data/exports/` ذخیره می‌شود

### 4. مشاهده گزارش‌ها و لاگ‌ها

1. از منوی سمت چپ گزینه **"گزارش‌ها"** را انتخاب کنید
2. فیلترهای زمانی یا نوع فرآیند را اعمال کنید
3. جزئیات هر عملیات را مشاهده کنید
4. خطاها و هشدارها را بررسی کنید

## ⚙️ تنظیمات پیشرفته

### تنظیم اتصال دیتابیس

فایل: `app/models/base.py`

```python
DATABASE_URL = os.getenv('DATABASE_URL', 'postgresql://user:pass@localhost/db_name')
```

### تنظیم Batch Size

فایل: `.env`

```env
BATCH_SIZE=1000  # تعداد ردیف‌های هر batch
MAX_WORKERS=4    # تعداد Thread ها
```

### تنظیم Retry

فایل: `app/core/google_sheets.py`

```python
@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
```

## 🐛 رفع مشکلات رایج

### خطای اتصال به دیتابیس

```
خطا: could not connect to server
```

**راه‌حل:**
1. بررسی کنید سرویس PostgreSQL در حال اجرا باشد
2. تنظیمات `DATABASE_URL` در `.env` را بررسی کنید
3. دسترسی کاربر به دیتابیس را بررسی کنید

### خطای Google Sheets API

```
خطا در احراز هویت با Google Sheets
```

**راه‌حل:**
1. مسیر فایل `credentials.json` را بررسی کنید
2. از فعال بودن Google Sheets API اطمینان حاصل کنید
3. دسترسی Service Account به فایل‌ها را بررسی کنید

### خطای Import Module

```
ModuleNotFoundError: No module named 'xxx'
```

**راه‌حل:**
```powershell
pip install -r requirements.txt --force-reinstall
```

## 📊 مدیریت دیتابیس

### Backup دیتابیس

```powershell
# Windows
pg_dump -U gt_land_user -d gt_land -f backup_$(Get-Date -Format 'yyyyMMdd').sql
```

### Restore دیتابیس

```powershell
psql -U gt_land_user -d gt_land -f backup_20251105.sql
```

### پاک کردن داده‌ها

```python
# احتیاط: این عملیات برگشت‌ناپذیر است!
python -c "from app.models import drop_db, init_db; drop_db(); init_db()"
```

## 🔒 امنیت

- ✅ فایل `.env` را هرگز commit نکنید
- ✅ فایل `credentials.json` را محرمانه نگه دارید
- ✅ از رمز عبور قوی برای دیتابیس استفاده کنید
- ✅ دسترسی Service Account را محدود کنید
- ✅ به صورت دوره‌ای backup بگیرید

## 📝 لاگ‌ها

لاگ‌ها در دو مکان ذخیره می‌شوند:

1. **فایل‌های لاگ:** `logs/app_YYYY-MM-DD.log`
2. **جدول دیتابیس:** `process_logs`

سطوح لاگ:
- DEBUG: اطلاعات دیباگ
- INFO: اطلاعات عمومی
- SUCCESS: عملیات موفق
- WARNING: هشدارها
- ERROR: خطاها
- CRITICAL: خطاهای بحرانی

## 🚧 توسعه آینده

- [ ] پشتیبانی از چند کاربر
- [ ] رابط API RESTful
- [ ] پنل وب
- [ ] اعلان‌های Push
- [ ] گزارش‌های پیشرفته با نمودار
- [ ] Export به فرمت‌های دیگر (CSV, PDF)
- [ ] Scheduler خودکار
- [ ] تست‌های واحد (Unit Tests)

## 📞 پشتیبانی

در صورت بروز مشکل:

1. لاگ‌های برنامه را بررسی کنید: `logs/`
2. جدول `process_logs` را چک کنید
3. تنظیمات `.env` را بررسی کنید

## 📜 مجوز

این پروژه برای استفاده داخلی شرکت طراحی شده است.

---

**نسخه:** 1.0.0  
**تاریخ:** 2025-11-05  
**توسعه‌دهنده:** GT-Land Team
