# 📘 راهنمای سیستم Template پیشرفته

## نسخه 9 - GT-Land Manager

---

## 🎯 ویژگی‌های جدید

### 1️⃣ خواندن خودکار ستون‌های Google Sheets
سیستم **به صورت خودکار** ستون‌های واقعی را از Google Sheets می‌خواند:
- ✅ دیگر نیازی به وارد کردن دستی نام ستون‌ها نیست
- ✅ داده‌های واقعی از Google Sheets نمایش داده می‌شود
- ✅ اگر اتصال به Google برقرار نشود، از `column_mappings` ذخیره شده استفاده می‌کند

### 2️⃣ انواع Template با قابلیت Custom
شما می‌توانید از **4 نوع** Template استفاده کنید:

| نوع | توضیحات | مثال استفاده |
|-----|---------|--------------|
| **تایپ 1** | سامانه مؤدیان | فایل‌های اداری و مالیاتی |
| **تایپ 2** | گزارش فروش | گزارش‌های روزانه/ماهانه |
| **تایپ 3** | صورت حساب | فاکتورها و صورتحساب‌ها |
| **💠 Custom** | نام دلخواه شما | هر نوع فایل دیگر |

#### نحوه استفاده از Custom:
1. در صفحه "تنظیمات Template"، از لیست کشویی "💠 Custom - نام دلخواه" را انتخاب کنید
2. فیلد "نام Custom" ظاهر می‌شود
3. نام دلخواه خود را وارد کنید (مثلاً: "تایپ ویژه شرکت ABC")
4. Template با نام `Custom: تایپ ویژه شرکت ABC` ذخیره می‌شود

---

## 🔧 نحوه کار با Template Manager

### مرحله 1: انتخاب فایل Excel
![Select File](images/step1.png)
1. روی دکمه "📂 انتخاب فایل Excel" کلیک کنید
2. فایل Excel نمونه خود را انتخاب کنید
3. سیستم **به صورت خودکار** فایل را تحلیل می‌کند

**نمونه تحلیل:**
```
📊 تحلیل فایل Excel:
📁 نام فایل: sample_template.xlsx
📄 تعداد Sheet ها: 1

==================================================
📋 Sheet: Sheet1
==================================================
📊 تعداد ستون‌ها: 5
📝 عناوین ستون‌ها:
   A: ردیف
   B: نام مشتری
   C: تاریخ
   D: مبلغ
   E: توضیحات
```

### مرحله 2: مشاهده تحلیل
سیستم اطلاعات زیر را نمایش می‌دهد:
- 📁 نام فایل
- 📄 تعداد Sheet ها
- 📊 تعداد ستون‌های هر Sheet
- 📝 نام هر ستون با حرف Excel آن (A, B, C, ...)

### مرحله 3: انتخاب Google Sheets
![Select Sheets](images/step3.png)

**توجه مهم:** 
- سیستم **ستون‌های واقعی** را از Google Sheets می‌خواند
- اگر Google Sheet شما دارای ستون‌های زیر است:
  - `ستون یک` (Column A)
  - `ستون دو` (Column B)
  - `آماده استخراج` (Column C)
  - `استخراج شد` (Column D)
- همین نام‌ها در صفحه بعدی نمایش داده می‌شود ✅

**قبلاً (مشکل):**
```
❌ Column_A
❌ Column_B
❌ Column_C
```

**حالا (درست):**
```
✅ ستون یک
✅ ستون دو
✅ آماده استخراج
✅ استخراج شد
```

### مرحله 4: Drag & Drop Mapping
![Mapping](images/step4.png)

این قسمت **قلب سیستم** است:

#### سمت راست: ستون‌های Google Sheet
- 📊 نام ستون‌های **واقعی** از Google Sheet شما
- می‌توانید آنها را بکشید (Drag)

#### سمت چپ: ستون‌های Excel
- 📄 ستون‌های فایل Excel نمونه
- می‌توانید روی آنها رها کنید (Drop)

#### نحوه Mapping:
1. یک ستون Google Sheet را انتخاب کنید
2. آن را بکشید
3. روی ستون Excel مربوطه رها کنید
4. ✅ Mapping ایجاد شد!

**مثال واقعی:**
```
ستون Google Sheet         →  ستون Excel
────────────────────────────────────────
📊 نام مشتری              →  B
📊 تاریخ                  →  C  
📊 مبلغ                   →  D
📊 توضیحات                →  E
```

#### افزودن Formula:
1. روی یک ستون Excel که Mapping دارد کلیک کنید
2. روی دکمه "⚡ افزودن Formula" کلیک کنید
3. نوع Formula را انتخاب کنید:
   - **Merge** - ادغام چند ستون
   - **Trim** - حذف فاصله‌ها
   - **Upper/Lower** - تبدیل حروف
   - **Replace** - جایگزینی متن
   - **Math** - عملیات ریاضی (+, -, *, /)
   - **Date Format** - قالب‌بندی تاریخ
   - **IF Condition** - شرط
   - **Substring** - برش متن
   - **Number Format** - قالب‌بندی عدد

### مرحله 5: تنظیمات و ذخیره
![Settings](images/step5.png)

#### 📝 نام Template
نام یکتای Template را وارد کنید (مثلاً: "Template سامانه مؤدیان 1402")

#### 🏷️ نوع Template
یکی از 4 نوع را انتخاب کنید:
- **تایپ 1 - سامانه مؤدیان**: برای فایل‌های اداری
- **تایپ 2 - گزارش فروش**: برای گزارش‌های فروش
- **تایپ 3 - صورت حساب**: برای فاکتورها
- **💠 Custom - نام دلخواه**: برای استفاده‌های خاص

##### استفاده از Custom Type:
```
1. انتخاب: "💠 Custom - نام دلخواه"
2. فیلد جدید ظاهر می‌شود: "📝 نام Custom:"
3. نام را وارد کنید (مثلاً: "تایپ ویژه پروژه X")
4. ذخیره → Template با نام "Custom: تایپ ویژه پروژه X" ذخیره می‌شود
```

#### 📄 توضیحات
توضیحات اختیاری درباره Template

#### 📊 خلاصه Template
سیستم خلاصه‌ای از Template شما نمایش می‌دهد:
- 📁 نام فایل Excel
- 📊 تعداد ستون‌های Excel
- ✅ تعداد Mapping ها
- ⚡ تعداد Formula ها

---

## 🎨 تصاویر مقایسه

### قبل (داده‌های نمونه ❌)
```
Google Sheets → برنامه:
ستون یک      → Column_A ❌
ستون دو       → Column_B ❌
تاریخ         → Column_C ❌
```

### بعد (داده‌های واقعی ✅)
```
Google Sheets → برنامه:
ستون یک      → ستون یک ✅
ستون دو       → ستون دو ✅  
تاریخ         → تاریخ ✅
```

---

## 🔍 عیب‌یابی

### مشکل: ستون‌های Google Sheet نمایش داده نمی‌شود
**حل:**
1. مطمئن شوید Google Sheet در منوی اصلی تنظیم شده است
2. بررسی کنید فایل `credentials.json` در مسیر `config/` وجود دارد
3. لاگ برنامه را بررسی کنید: `logs/app.log`

### مشکل: داده‌های نمونه (Column_A) نمایش داده می‌شود
**حل:**
1. اتصال به Google Sheets را بررسی کنید
2. مجوزهای دسترسی Service Account را بررسی کنید
3. اگر اتصال برقرار نشد، سیستم از `column_mappings` ذخیره شده استفاده می‌کند

### مشکل: نمی‌توانم Custom Type بسازم
**حل:**
1. از لیست کشویی "نوع Template"، گزینه "💠 Custom - نام دلخواه" را انتخاب کنید
2. منتظر بمانید تا فیلد "نام Custom" ظاهر شود
3. نام دلخواه خود را وارد کنید
4. اگر فیلد خالی باشد، پیام خطا دریافت می‌کنید

---

## 📚 مثال کامل

### سناریو: ایجاد Template برای فروش ماهانه

#### 1. فایل Excel نمونه
```
| ردیف | نام مشتری | تاریخ | مبلغ | توضیحات |
|------|-----------|-------|------|---------|
| 1    | احمد      | ...   | ...  | ...     |
```

#### 2. Google Sheet شما
```
| ستون یک | ستون دو | تست ستون 10 | ... | آماده استخراج | استخراج شد |
|---------|---------|-------------|-----|----------------|-----------|
| تست ... | 9 ...   | 10 ...      | ... | ☑               | ☑          |
```

#### 3. Mapping پیشنهادی
```
ستون Google              →  ستون Excel
──────────────────────────────────────
📊 ستون یک              →  B (نام مشتری)
📊 تست ستون 10          →  C (تاریخ)
📊 ستون دو              →  D (مبلغ)
```

#### 4. نوع Template
- انتخاب: "تایپ 2 - گزارش فروش"
- یا: "💠 Custom" با نام "فروش ماهانه شرکت ABC"

#### 5. نتیجه
✅ Template با موفقیت ساخته شد
✅ هر بار که Export می‌کنید، داده‌ها **به طور خودکار** از Google Sheets خوانده شده و در فرمت Excel ذخیره می‌شوند

---

## 🚀 نکات پیشرفته

### 1. استفاده از Formula برای تبدیل داده‌ها
**مثال: تبدیل عدد به فرمت پولی**
```
ستون: مبلغ
Formula: Number Format
Format: #,##0 تومان
نتیجه: 1000000 → 1,000,000 تومان
```

### 2. ادغام چند ستون
**مثال: نام کامل از نام و نام خانوادگی**
```
ستون: نام کامل
Formula: Merge
Source 1: نام
Source 2: نام خانوادگی
Separator: " "
نتیجه: "احمد" + "رضایی" → "احمد رضایی"
```

### 3. شرط IF برای فیلتر کردن
**مثال: نمایش وضعیت بر اساس مبلغ**
```
ستون: وضعیت
Formula: IF Condition
Condition: مبلغ > 1000000
True: "بالا"
False: "پایین"
```

---

## 📝 لاگ تغییرات (Ver 9)

### ✨ ویژگی‌های جدید:
- ✅ خواندن خودکار ستون‌های واقعی از Google Sheets
- ✅ انواع Template: Type 1, 2, 3, Custom
- ✅ قابلیت نام‌گذاری Custom برای انواع Template
- ✅ نمایش داده‌های واقعی در Drag & Drop Widget
- ✅ تشخیص هوشمند وضعیت سلول‌ها (Checkbox, Dropdown, Unicode)
- ✅ پشتیبانی از 9 نوع Formula

### 🐛 رفع مشکلات:
- ✅ مشکل نمایش داده‌های نمونه به جای واقعی
- ✅ مشکل Crash در Drag & Drop
- ✅ مشکل باز شدن Dialog قدیمی به جای جدید
- ✅ مشکل رنگ‌بندی ناهماهنگ

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. لاگ‌های برنامه را بررسی کنید: `logs/app.log`
2. گزارش‌های خطا را چک کنید
3. از منوی "تست اتصال" در Sheet Config استفاده کنید

---

**توسعه یافته توسط:** GT-Land Manager Team  
**نسخه:** 9.0  
**تاریخ:** November 2025

---

## 🎓 خلاصه

| قابلیت | وضعیت | توضیحات |
|--------|-------|---------|
| خواندن ستون‌های واقعی | ✅ فعال | از Google Sheets API |
| Template Type 1-3 | ✅ فعال | سه نوع از پیش تعریف شده |
| Custom Template Type | ✅ فعال | نام‌گذاری دلخواه |
| Drag & Drop Mapping | ✅ فعال | کشیدن و رها کردن |
| Formula Builder | ✅ فعال | 9 نوع Formula |
| Smart Cell Detection | ✅ فعال | Checkbox, Dropdown, Unicode |

**همه چیز آماده است! 🚀**
