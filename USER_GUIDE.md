# 📖 راهنمای کاربری GT-Land Manager# 📖 راهنمای کامل استفاده از GT-Land Manager



> راهنمای ساده برای کاربران عادی> **توسعه‌دهنده:** علیرضا حامد  

> **نسخه:** 1.0.0

---

---

## 🎯 شروع سریع

## 📋 فهرست مطالب

### نصب و اجرا

1. [پیش‌نیازها](#پیش‌نیازها)

1. **نصب Python** (اگر نصب نیست)2. [راه‌اندازی اولیه](#راه‌اندازی-اولیه)

   - دانلود از python.org3. [افزودن Google Sheet](#افزودن-google-sheet)

   - نسخه 3.10 یا بالاتر4. [استخراج داده](#استخراج-داده)

5. [خروجی Excel](#خروجی-excel)

2. **اجرای برنامه:**6. [تنظیمات](#تنظیمات)

   - دوبار کلیک روی `GT-Land.bat`7. [عیب‌یابی](#عیب‌یابی)

   - یا اجرای دستور: `python app/main.py`

---

---

## 1️⃣ پیش‌نیازها

## 📋 راهنمای تصویری

### ✅ چیزهایی که قبلاً باید آماده کنید:

### 1️⃣ افزودن Google Sheet

- [x] فایل `credentials.json` از Google Cloud Console

**مسیر:** تب "📋 مدیریت شیت‌ها" → دکمه "➕ افزودن شیت جدید"- [x] حداقل یک Google Sheet با ساختار مناسب

- [x] Share کردن Google Sheet با Service Account Email

#### مراحل:

### 📧 Service Account Email شما:

1. **نام شیت** (مثال: "فروش ماهانه")```

2. **Sheet ID** - از آدرس Google Sheet کپی کنید:gt-land-service@gt-land-manager.iam.gserviceaccount.com

   ``````

   https://docs.google.com/spreadsheets/d/[این قسمت را کپی کنید]/edit

   ```> ⚠️ **نکته:** اگر Google Sheets را راه‌اندازی نکرده‌اید، ابتدا فایل `GOOGLE_SETUP.md` را مطالعه کنید.

3. **نام برگه** (مثال: "Sheet1")

4. **محدوده** (مثال: "A1:Z")---



5. **Mapping ستون‌ها** - نام ستون در Sheet → نام در دیتابیس:## 2️⃣ راه‌اندازی اولیه

   ```

   کد محصول → product_code### مرحله 1: اجرای نرم‌افزار

   نام محصول → product_name

   قیمت → price```powershell

   تاریخ → date.\GT-Land.bat

   ``````



6. کلیک **"💾 ذخیره"**یا:



---```powershell

.\venv\Scripts\python.exe app\main.py

### 2️⃣ استخراج داده‌ها```



**مسیر:** تب "📥 استخراج داده"### مرحله 2: بررسی اتصال دیتابیس



#### مراحل:وقتی نرم‌افزار باز می‌شود:

- باید پیام "✅ اتصال به دیتابیس برقرار است" را ببینید

1. بررسی آمار:- Dashboard باید آماده باشد

   - تعداد Sheet های فعال

   - آخرین استخراج---

   - تعداد رکوردها

## 3️⃣ افزودن Google Sheet

2. کلیک **"▶️ شروع استخراج"**

### 📌 ساختار مورد نیاز Google Sheet

3. صبر کنید تا استخراج تمام شود

Google Sheet شما باید دارای این ساختار باشد:

4. در صورت وجود داده تکراری:

   - **رد کردن**: داده جدید نادیده گرفته شود| A | B | C | D | E | F | G | **H (Ready)** | **I (Extracted)** |

   - **جایگزینی**: داده قدیمی پاک و جدید ذخیره شود|---|---|---|---|---|---|---|---------------|-------------------|

   - **هر دو**: هر دو داده ذخیره شوند| ستون 1 | ستون 2 | ستون 3 | ... | ... | ... | ... | TRUE/FALSE | TRUE/FALSE |



---**توضیحات ستون‌ها:**

- **A, B, C, ..., G**: داده‌های اصلی شما (مثلاً: کد، نام، مبلغ، تاریخ، ...)

### 3️⃣ ایجاد Template برای Export- **H (Ready)**: برای مشخص کردن رکوردهای آماده برای استخراج

  - `TRUE` = این سطر آماده است

**مسیر:** تب "📤 Export و داده‌ها" → دکمه "🗂️ مدیریت Template ها"  - `FALSE` = این سطر آماده نیست

- **I (Extracted)**: برای مشخص کردن رکوردهای استخراج شده

#### مراحل:  - `TRUE` = قبلاً استخراج شده

  - `FALSE` = هنوز استخراج نشده

1. کلیک **"➕ افزودن Template جدید"**

### 📝 مراحل افزودن Sheet جدید

2. **نام Template** (مثال: "گزارش فروش ماهانه")

#### گام 1: رفتن به تب "🗂️ مدیریت شیت‌ها"

3. **انتخاب فایل Excel:**

   - یک فایل Excel آماده کنید1. نرم‌افزار را باز کنید

   - کلیک "📁 انتخاب فایل"2. روی تب **"🗂️ مدیریت شیت‌ها"** کلیک کنید

3. روی دکمه **"➕ افزودن شیت جدید"** کلیک کنید

4. **تنظیمات:**

   - نام Worksheet مقصد#### گام 2: پر کردن فرم

   - سطر شروع (معمولاً 2)

   - ستون شروع (معمولاً 1)یک فرم باز می‌شود با این فیلدها:



5. **Mapping ستون‌ها:**##### 📋 اطلاعات اصلی

   - افزودن ردیف

   - انتخاب ستون دیتابیس**1. نام:**

   - انتخاب ستون Excel (مثال: A، B، C یا نام ستون)- یک نام دلخواه برای شناسایی

- مثال: `فروش تهران`

6. کلیک **"💾 ذخیره"**

**2. آدرس شیت:**

---- لینک کامل Google Sheet

- مثال:

### 4️⃣ Export به Excel```

https://docs.google.com/spreadsheets/d/1hgKx5_EpIn-EnIac2RW-6lhQDbZ2KtjlMFH3XKjdOLA/edit?gid=0#gid=0

**مسیر:** تب "📤 Export و داده‌ها"```



#### روش 1: Export ساده**3. نام برگه:**

- نام Worksheet داخل Google Sheet

1. کلیک **"📤 Export جدید"**- پیش‌فرض: `Sheet1`

2. انتخاب Sheet- اگر برگه دیگری دارید (مثلاً `Sales` یا `Data`), نام آن را بنویسید

3. انتخاب محدوده زمانی (اختیاری)

4. انتخاب مکان ذخیره##### 🎯 ستون‌های کنترل

5. کلیک **"ذخیره"**

**4. ستون آماده:**

#### روش 2: با Template- ستونی که نشان می‌دهد رکورد آماده است

- مقدار پیش‌فرض: `H`

1. از لیست Template یکی را انتخاب کنید- در این ستون باید `TRUE` یا `FALSE` نوشته شود

2. انتخاب Sheet

3. محدوده زمانی (اختیاری)**5. ستون استخراج شده:**

4. کلیک **"Export با Template"**- ستونی که نشان می‌دهد رکورد استخراج شده است

5. فایل در پوشه `data/exports` ذخیره می‌شود- مقدار پیش‌فرض: `I`

- نرم‌افزار بعد از استخراج این ستون را `TRUE` می‌کند

---

##### 🔑 ستون‌های کلید یکتا

### 5️⃣ مشاهده گزارش‌ها

**6. ستون‌های کلید یکتا:**

**مسیر:** تب "📈 گزارش‌ها"- ستون‌هایی که **ترکیب آنها** یکتا است

- با **کامای انگلیسی** جدا کنید

#### انواع گزارش:- مثال: `A,B,C`

- این ستون‌ها برای جلوگیری از تکراری شدن رکوردها استفاده می‌شوند

- **خلاصه کلی**: آمار کلی سیستم

- **بر اساس Sheet**: آمار هر Sheet##### 📥 ستون‌های استخراج (مهم!)

- **بر اساس زمان**: آمار روزانه/ماهانه

- **Template ها**: آمار استفاده از Template ها**7. ستون‌های مورد نیاز برای استخراج:**

- فقط ستون‌هایی که می‌خواهید استخراج شوند

---- با **کامای انگلیسی** جدا کنید

- مثال: `A,B,C,D,E`

## ⚙️ تنظیمات- **اگر خالی بگذارید = همه ستون‌ها استخراج می‌شوند**



**مسیر:** تب "⚙️ تنظیمات"> 💡 **چرا فقط برخی ستون‌ها؟**

> - سرعت بیشتر

### تنظیمات مهم:> - حجم کمتر در دیتابیس

> - فقط داده‌های مورد نیاز را ذخیره کنید

1. **تنظیمات Google Sheets:**

   - مسیر فایل Credentials##### 🗺️ نقشه ستون‌ها (اختیاری)

   - تست اتصال

**8. نقشه ستون‌ها:**

2. **تنظیمات دیتابیس:**- برای نام‌گذاری ستون‌ها به فارسی

   - Backup خودکار (فعال/غیرفعال)- فرمت: JSON

   - تعداد روزهای نگهداری Archive- مثال:

   - بهینه‌سازی دیتابیس (Vacuum)```json

{

3. **تنظیمات Export:**  "A": "کد",

   - مسیر پیش‌فرض ذخیره  "B": "نام",

   - فرمت نام فایل  "C": "قیمت",

  "D": "تاریخ",

---  "E": "توضیحات"

}

## 🔧 رفع مشکلات رایج```



### مشکل 1: خطای احراز هویت Google##### ✅ تنظیمات اضافی



**علامت:** `Error: Unable to authenticate`**9. وضعیت:**

- چک‌باکس **"✅ فعال"**: اگر فعال باشد، این شیت در استخراج استفاده می‌شود

**راه‌حل:**- **سطر شروع**: از کدام سطر شروع شود (پیش‌فرض: 2 = بعد از هدر)

1. بررسی فایل `config/credentials.json`

2. اطمینان از اشتراک Sheet با Service Account#### گام 3: ذخیره

3. تست اتصال از تنظیمات

- روی دکمه **"💾 ذخیره"** کلیک کنید

### مشکل 2: داده‌ها استخراج نمی‌شوند- اگر همه چیز درست باشد، پیام **"✅ تنظیمات با موفقیت ذخیره شد"** نمایش داده می‌شود



**راه‌حل:**---

1. بررسی اتصال اینترنت

2. بررسی صحت Sheet ID## 4️⃣ استخراج داده

3. بررسی نام برگه (Sheet Name)

4. بررسی محدوده (Range)### مرحله 1: رفتن به تب "📥 استخراج داده"



### مشکل 3: Export کار نمی‌کند### مرحله 2: بررسی شیت‌های فعال



**راه‌حل:**- لیست شیت‌های فعال نمایش داده می‌شود

1. بررسی وجود فایل Template- فقط شیت‌هایی که **✅ فعال** هستند در این لیست هستند

2. بررسی Mapping ستون‌ها

3. بررسی دسترسی نوشتن در پوشه مقصد### مرحله 3: شروع استخراج



### مشکل 4: برنامه باز نمی‌شود1. روی دکمه **"▶️ شروع استخراج"** کلیک کنید

2. فرآیند استخراج شروع می‌شود:

**راه‌حل:**   - اتصال به Google Sheets

1. بررسی نصب Python   - خواندن رکوردهای آماده (`Ready=TRUE` و `Extracted=FALSE`)

2. اجرای `pip install -r requirements.txt`   - چک کردن تکراری نبودن

3. مشاهده لاگ‌ها در `logs/app.log`   - ذخیره در دیتابیس

   - علامت‌گذاری رکورد به عنوان استخراج شده (`Extracted=TRUE`)

---

### مرحله 4: مشاهده نتایج

## 💡 نکات کاربردی

**لاگ کنسول (سبز/سیاه):**

### نکته 1: سرعت بخشیدن به استخراج```

- Sheet های غیرفعال را غیرفعال کنید✅ شروع استخراج از: فروش تهران

- محدوده (Range) را کوچک‌تر کنید📊 تعداد رکوردهای آماده: 5

- از Unique Key مناسب استفاده کنید✅ رکورد جدید اضافه شد: Row 2

✅ رکورد جدید اضافه شد: Row 3

### نکته 2: مدیریت بهتر Template ها⚠️ رکورد تکراری رد شد: Row 4

- نام‌گذاری واضح✅ استخراج کامل شد. تعداد: 4 رکورد

- افزودن توضیحات```

- غیرفعال کردن Template های قدیمی

### ⚠️ نکات مهم

### نکته 3: Backup منظم

- Backup خودکار را فعال کنید1. **استخراج فقط رکوردهای جدید:**

- Backup دستی قبل از تغییرات بزرگ   - `Ready = TRUE` ✅

- نگهداری Backup خارج از برنامه   - `Extracted = FALSE` ✅



### نکته 4: بهینه‌سازی دیتابیس2. **بعد از استخراج:**

- هر ماه یک بار Vacuum   - ستون `Extracted` به `TRUE` تغییر می‌کند

- Archive داده‌های قدیمی   - دیگر در دفعات بعدی استخراج نمی‌شوند

- حذف Backup های بسیار قدیمی

3. **جلوگیری از تکرار:**

---   - بر اساس ستون‌های کلید یکتا

   - اگر رکورد قبلاً وجود داشته باشد، رد می‌شود

## 📞 راهنمای بیشتر

---

برای اطلاعات کامل‌تر:

- **[COMPLETE_GUIDE.md](COMPLETE_GUIDE.md)** - راهنمای جامع## 5️⃣ خروجی Excel



برای پشتیبانی:### مرحله 1: ایجاد تمپلیت (اولین بار)

- **ایمیل:** support@gtland.com

- **Telegram:** @GTLandSupport```powershell

.\venv\Scripts\python.exe create_templates.py

---```



**نسخه:** 2.2.0  این دستور فایل‌های نمونه Excel در پوشه `templates/` ایجاد می‌کند.

**آخرین بروزرسانی:** نوامبر 2025

### مرحله 2: رفتن به تب "📤 خروجی Excel"

🎉 **موفق باشید!**

### مرحله 3: انتخاب تمپلیت

1. از لیست کشویی یک تمپلیت انتخاب کنید
2. روی دکمه **"📥 انتخاب فایل خروجی"** کلیک کنید
3. مسیر ذخیره فایل را انتخاب کنید

### مرحله 4: صادر کردن

- روی دکمه **"🚀 شروع Export"** کلیک کنید
- فایل Excel در مسیر انتخابی ذخیره می‌شود

---

## 6️⃣ تنظیمات

### تب "⚙️ تنظیمات"

#### 1️⃣ تب Google Sheets

**تست اتصال:**
- روی **"🔍 تست اتصال"** کلیک کنید
- باید پیام **"✅ اتصال برقرار است"** را ببینید

**تنظیم مجدد Credentials:**
- روی **"🔄 تنظیم مجدد"** کلیک کنید
- فایل `credentials.json` جدید را انتخاب کنید

#### 2️⃣ تب دیتابیس

**بکاپ دیتابیس:**
- روی **"💾 ایجاد بکاپ"** کلیک کنید
- فایل بکاپ در `data/backups/` ذخیره می‌شود

**آرشیو داده‌ها:**
- برای کاهش حجم دیتابیس اصلی
- روی **"📦 آرشیو داده‌ها"** کلیک کنید
- داده‌های قدیمی به دیتابیس آرشیو منتقل می‌شوند

**بازیابی:**
- روی **"♻️ بازیابی"** کلیک کنید
- فایل بکاپ را انتخاب کنید

#### 3️⃣ تب لاگ‌ها

**مشاهده لاگ‌ها:**
- تمام عملیات در فایل‌های لاگ ذخیره می‌شوند
- مسیر: `logs/app_YYYY-MM-DD.log`

**پاک کردن لاگ‌ها:**
- روی **"🗑️ پاک کردن لاگ‌ها"** کلیک کنید

#### 4️⃣ تب عمومی

**زبان:**
- فعلاً فقط فارسی

**تم:**
- روشن / تیره (در نسخه‌های بعدی)

---

## 7️⃣ عیب‌یابی

### ❌ خطا: "Import PyQt6 could not be resolved"

**علت:** VS Code پایتون venv را تشخیص نداده

**راه‌حل:**
```powershell
# 1. محیط را فعال کنید
.\venv\Scripts\Activate.ps1

# 2. دوباره PyQt6 را نصب کنید
pip install PyQt6

# 3. VS Code را ریستارت کنید
```

### ❌ خطا: "no such column: sheet_configs.columns_to_extract"

**علت:** دیتابیس قدیمی است

**راه‌حل:**
```powershell
# دیتابیس را دوباره بسازید
.\venv\Scripts\python.exe setup_database.py
```

### ❌ خطا: "Permission denied" - Google Sheets

**علت:** Sheet را با Service Account share نکرده‌اید

**راه‌حل:**
1. Google Sheet را باز کنید
2. روی **Share** کلیک کنید
3. این Email را اضافه کنید:
   ```
   gt-land-service@gt-land-manager.iam.gserviceaccount.com
   ```
4. دسترسی: **Editor**

### ❌ خطا: "API not enabled"

**علت:** API های گوگل فعال نیستند

**راه‌حل:**
1. به [Google Cloud Console](https://console.cloud.google.com) بروید
2. پروژه خود را انتخاب کنید
3. **APIs & Services** > **Enable APIs**
4. فعال کنید:
   - Google Sheets API
   - Google Drive API

### ❌ خطا: "too many values to unpack (expected 2)"

**علت:** مشکل در کد (فقط برای توسعه‌دهندگان)

**راه‌حل:**
- فایل `sheet_config_dialog.py` را بررسی کنید
- از `Tuple[bool, str]` به جای `tuple[bool, str]` استفاده کنید
- `from typing import Tuple` را اضافه کنید

### ⚠️ هشدار: Font DirectWrite

**علت:** مشکل فونت ویندوز (غیرمهم)

**راه‌حل:**
- نیازی به کاری نیست
- نرم‌افزار بدون مشکل کار می‌کند

---

## 📞 پشتیبانی

اگر مشکلی دارید:

1. **لاگ‌ها را بررسی کنید:**
   ```powershell
   Get-Content logs\app_*.log -Tail 50
   ```

2. **تست اتصال Google Sheets:**
   ```powershell
   .\venv\Scripts\python.exe test_google_connection.py
   ```

3. **بررسی دیتابیس:**
   ```powershell
   .\venv\Scripts\python.exe setup_database.py
   ```

---

## 🎯 نکات کاربردی

### ✅ بهترین روش‌ها (Best Practices)

1. **قبل از استخراج:**
   - Google Sheet را چک کنید
   - مطمئن شوید ستون‌های `Ready` و `Extracted` درست هستند

2. **ستون‌های استخراج:**
   - فقط ستون‌های مورد نیاز را انتخاب کنید
   - سرعت را افزایش می‌دهد

3. **بکاپ منظم:**
   - هر روز یک بکاپ بگیرید
   - قبل از عملیات بزرگ حتماً بکاپ بگیرید

4. **آرشیو:**
   - هر ماه داده‌های قدیمی را آرشیو کنید
   - دیتابیس سبک‌تر = سریع‌تر

5. **لاگ‌ها:**
   - هر هفته لاگ‌های قدیمی را پاک کنید

### 🚀 گردش کار پیشنهادی

```
1. راه‌اندازی Google Sheets
   ↓
2. افزودن Sheet در نرم‌افزار
   ↓
3. در Google Sheet رکوردها را Ready کنید (H = TRUE)
   ↓
4. استخراج داده در نرم‌افزار
   ↓
5. بررسی رکوردها در تب "گزارش‌ها"
   ↓
6. صادر کردن Excel (در صورت نیاز)
   ↓
7. بکاپ هفتگی
```

---

## 📚 مستندات مرتبط

- **GOOGLE_SETUP.md** - راه‌اندازی Google Sheets API
- **QUICKSTART.md** - شروع سریع
- **DEVELOPER.md** - راهنمای توسعه‌دهندگان
- **ARCHITECTURE.md** - معماری نرم‌افزار

---

**✨ موفق باشید!**

> توسعه‌دهنده: **علیرضا حامد**  
> پروژه: **GT-Land Manager v1.0.0**
